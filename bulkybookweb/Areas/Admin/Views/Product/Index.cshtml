@model IEnumerable<Bulky.Models.Product>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container p-3">
    <div class="row pt-4 pb-5">
        <div class="col-6">
            <h3 class="text-primary"> Product list</h3>
        </div>
        <div class="col-6 text-end">
            <a asp-asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-circle-fill"></i> &nbsp;  create new category
            </a>
        </div>
    </div>



    <table id="prTable" class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>  Title </th>
                
                <th> ISBN</th>
                <th> Price</th>
                <th> Author</th>
                <th> Category</th>
                <th> </th>
            </tr>

        </thead>
    </table>
      @*  <tbody>
            @foreach (var product in (IEnumerable<Product>)ViewData["Products"])
            {
                <tr>
                        <!-- Accessing products -->
                    <td>@product.Title </td>
                    <td> @product.Description</td>
                    <td> @product.Listprice</td>
                    <td> @product.ISBN</td>
                   
                        
                        <!-- Accessing categories -->
                        @foreach (var category in (IEnumerable<Category>)ViewData["Categories"])
                        {
                        <td>  @product.Category.name</td>
                        break;
                    }
                 
                    <td style="width:220px">
                        <div>
                            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary mx-2" asp-route-id="@product.Id">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>&nbsp;
                            <a asp-controller="Product" asp-action="Delete" class="btn btn-danger" asp-route-id="@product.Id">
                                <i class="bi bi-trash-fill"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
                
            }
        </tbody>*@




</div>

@section Scripts{
    <script src="~/js/product.js"></script>
}